#include <stdio.h>
int main( ){
    // Predict the output
    int j;          // Garbage value -> Random value can be assinged
    printf("\n%d",j);
    while (j<=10){
        printf("\n%d",j);
        j = j + 1;
    }
    
    return 0;
}