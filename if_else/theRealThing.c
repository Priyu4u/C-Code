#include <stdio.h>
int main( ){
    /* if( condition ){
        code
     } */ 


    /* if( statement ){
        code
     } */

    if (3 + 2 % 5){   // it is a statement not condition
        printf("This works");
    }
    
    return 0;
}